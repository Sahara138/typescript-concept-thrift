// dynamically generalize
 const friends : string[] = ['Alice', 'Bob', 'Charlie'];

// we can use type alias to create a new name for a type, which can be used to define complex types or to give a more meaningful name to a type. In this case, we are creating a type alias called 'Friends' for an array of strings, which represents a list of friends.